---
interface Props {
	title: string;
	href: string;
	description: string;
	draft: boolean;
  date: date;
}

const { href, title, description, date, draft } = Astro.props;
---

<li class="">
		<a href={`/posts/${href}`} class="">
			<div class="hover:translate-x-2 transition-all flex flex-col">
				<span class="">{date.toLocaleDateString()}:</span>
				<h3 class="xerox font-display font-bold text-2xl">{title}</h3>
				{description && (
					<span class="opacity-50"> — {description}</span>
				)}
				{draft === true && (
					<span class="text-red-500">Draft</span>
				)}
			</div>
		</a>
</li>